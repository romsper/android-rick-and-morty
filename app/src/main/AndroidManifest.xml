<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".BaseApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_rick_and_morty"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_rick_and_morty_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidRickAndMorty">

        <activity android:name="com.romsper.android_rick_and_morty.NavGraphActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>